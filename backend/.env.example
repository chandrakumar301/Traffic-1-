# Backend server port
PORT=3001  # ✅ Fallback port for local dev, Render will override this automatically

# Frontend URL for CORS (change in production)
# Use your deployed frontend URL on Render or Vercel here
FRONTEND_URL=http://localhost:8080